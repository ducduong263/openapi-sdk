[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dnse-sdk"
version = "1.0.0"
description = "DNSE OpenAPI SDK (MarketPulse local install)"

[tool.setuptools.packages.find]
where = ["."]
include = ["dnse*"]
